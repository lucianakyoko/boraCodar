import { colors } from "@/styles/colors";
import { device } from "@/styles/devices";
import styled from "styled-components";

export const IDidWrapper = styled.div`
  padding: 1.6rem 0;
  
  display: flex;
  flex-direction: column;
  gap: .8rem;
`;