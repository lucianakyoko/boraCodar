import styled from 'styled-components';
import { colors } from '../../../styles/colors';

export const BiggerPlayer = styled.div`
  padding: 5.095rem 3.841rem;
  display: flex;
  flex-direction: column;
  gap: 2.881rem;

  background-color: ${colors.background[150]};
  border-radius: .6rem;
`;